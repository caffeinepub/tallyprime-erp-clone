require('dotenv').config();
const mysql=require('mysql2/promise');
async function migrate(){
  const c=await mysql.createConnection({host:process.env.DB_HOST||'localhost',port:parseInt(process.env.DB_PORT)||3306,user:process.env.DB_USER||'root',password:process.env.DB_PASSWORD||'',multipleStatements:true});
  const db=process.env.DB_NAME||'hisabkitab_pro';
  await c.query(`CREATE DATABASE IF NOT EXISTS \`${db}\`; USE \`${db}\`;`);
  const tables=[
    `CREATE TABLE IF NOT EXISTS users(id INT AUTO_INCREMENT PRIMARY KEY,username VARCHAR(100) UNIQUE NOT NULL,password_hash VARCHAR(255) NOT NULL,role ENUM('admin','accountant','auditor','viewer') DEFAULT 'viewer',full_name VARCHAR(200),email VARCHAR(200),phone VARCHAR(20),profile_image LONGTEXT,theme VARCHAR(50) DEFAULT 'default',custom_shortcuts TEXT,is_active TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS companies(id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(255) NOT NULL,financial_year_start VARCHAR(20),financial_year_end VARCHAR(20),currency VARCHAR(10) DEFAULT 'INR',gstin VARCHAR(20),address TEXT,pan VARCHAR(20),state_code VARCHAR(5),state_name VARCHAR(100),phone VARCHAR(20),email VARCHAR(200),website VARCHAR(255),logo LONGTEXT,brand_color VARCHAR(20),tagline VARCHAR(255),signature LONGTEXT,invoice_prefix VARCHAR(20) DEFAULT 'INV',owner VARCHAR(100),is_active TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS ledger_groups(id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(200) NOT NULL,parent_group_id INT,nature ENUM('DR','CR') DEFAULT 'DR',is_predefined TINYINT DEFAULT 0,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS ledgers(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200) NOT NULL,group_id INT,opening_balance DECIMAL(18,2) DEFAULT 0,balance_type ENUM('DR','CR') DEFAULT 'DR',phone VARCHAR(20),email VARCHAR(200),gstin VARCHAR(20),address TEXT,pan VARCHAR(20),bank_account VARCHAR(50),ifsc VARCHAR(20),bank_name VARCHAR(100),is_active TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS vouchers(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,voucher_type VARCHAR(50) NOT NULL,voucher_number INT NOT NULL,date DATETIME NOT NULL,narration TEXT,ref_number VARCHAR(100),party_ledger_id INT,created_by VARCHAR(100),status VARCHAR(30) DEFAULT 'posted',created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS voucher_entries(id INT AUTO_INCREMENT PRIMARY KEY,voucher_id INT NOT NULL,ledger_id INT NOT NULL,amount DECIMAL(18,2) NOT NULL,entry_type ENUM('DR','CR') NOT NULL,cost_centre_id INT,narration VARCHAR(500),FOREIGN KEY(voucher_id)REFERENCES vouchers(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS hsn_codes(id INT AUTO_INCREMENT PRIMARY KEY,code VARCHAR(20) UNIQUE NOT NULL,description TEXT,gst_rate DECIMAL(5,2) DEFAULT 0,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS gst_settings(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT UNIQUE NOT NULL,registration_type VARCHAR(50),state_code VARCHAR(10),state_name VARCHAR(100),FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS gst_vouchers(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,voucher_type VARCHAR(50) NOT NULL,voucher_number INT NOT NULL,date DATETIME NOT NULL,narration TEXT,party_name VARCHAR(255),party_gstin VARCHAR(20),place_of_supply VARCHAR(100),is_inter_state TINYINT DEFAULT 0,taxable_value DECIMAL(18,2) DEFAULT 0,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS gst_voucher_entries(id INT AUTO_INCREMENT PRIMARY KEY,gst_voucher_id INT NOT NULL,ledger_id INT NOT NULL,amount DECIMAL(18,2) NOT NULL,entry_type ENUM('DR','CR') NOT NULL,hsn_code VARCHAR(20),taxable_amount DECIMAL(18,2),cgst_rate DECIMAL(5,2),sgst_rate DECIMAL(5,2),igst_rate DECIMAL(5,2),cgst_amount DECIMAL(18,2),sgst_amount DECIMAL(18,2),igst_amount DECIMAL(18,2),cess_amount DECIMAL(18,2),FOREIGN KEY(gst_voucher_id)REFERENCES gst_vouchers(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS stock_groups(id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(200) NOT NULL,parent_group_id INT,unit VARCHAR(20),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS stock_items(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200) NOT NULL,stock_group_id INT,unit VARCHAR(20),opening_qty DECIMAL(18,4) DEFAULT 0,opening_rate DECIMAL(18,2) DEFAULT 0,opening_value DECIMAL(18,2) DEFAULT 0,gst_rate DECIMAL(5,2) DEFAULT 0,hsn_code VARCHAR(20),reorder_level DECIMAL(18,4) DEFAULT 0,description TEXT,barcode VARCHAR(100),is_active TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS stock_vouchers(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,voucher_type VARCHAR(50) NOT NULL,voucher_number INT NOT NULL,date DATETIME NOT NULL,narration TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS stock_voucher_entries(id INT AUTO_INCREMENT PRIMARY KEY,stock_voucher_id INT NOT NULL,stock_item_id INT NOT NULL,qty DECIMAL(18,4) NOT NULL,rate DECIMAL(18,2) NOT NULL,amount DECIMAL(18,2) NOT NULL,warehouse_from VARCHAR(100),warehouse_to VARCHAR(100),FOREIGN KEY(stock_voucher_id)REFERENCES stock_vouchers(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS employees(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200) NOT NULL,employee_code VARCHAR(50),department VARCHAR(100),designation VARCHAR(100),date_of_joining VARCHAR(20),pan VARCHAR(20),aadhaar VARCHAR(20),bank_account VARCHAR(30),bank_name VARCHAR(100),ifsc VARCHAR(20),pf_applicable TINYINT DEFAULT 0,esi_applicable TINYINT DEFAULT 0,dob VARCHAR(20),gender VARCHAR(10),phone VARCHAR(20),email VARCHAR(200),address TEXT,is_active TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS salary_structures(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,employee_id INT NOT NULL UNIQUE,basic DECIMAL(18,2) DEFAULT 0,hra DECIMAL(18,2) DEFAULT 0,da DECIMAL(18,2) DEFAULT 0,conveyance DECIMAL(18,2) DEFAULT 0,special_allowance DECIMAL(18,2) DEFAULT 0,other_allowances DECIMAL(18,2) DEFAULT 0,pf DECIMAL(18,2) DEFAULT 0,esi DECIMAL(18,2) DEFAULT 0,tds DECIMAL(18,2) DEFAULT 0,professional_tax DECIMAL(18,2) DEFAULT 0,other_deductions DECIMAL(18,2) DEFAULT 0)`,
    `CREATE TABLE IF NOT EXISTS payroll_vouchers(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,month INT NOT NULL,year INT NOT NULL,processed_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS payroll_entries(id INT AUTO_INCREMENT PRIMARY KEY,payroll_voucher_id INT NOT NULL,employee_id INT,employee_name VARCHAR(200),department VARCHAR(100),designation VARCHAR(100),basic DECIMAL(18,2) DEFAULT 0,hra DECIMAL(18,2) DEFAULT 0,da DECIMAL(18,2) DEFAULT 0,conveyance DECIMAL(18,2) DEFAULT 0,special_allowance DECIMAL(18,2) DEFAULT 0,other_allowances DECIMAL(18,2) DEFAULT 0,gross_earnings DECIMAL(18,2) DEFAULT 0,pf DECIMAL(18,2) DEFAULT 0,esi DECIMAL(18,2) DEFAULT 0,tds DECIMAL(18,2) DEFAULT 0,professional_tax DECIMAL(18,2) DEFAULT 0,other_deductions DECIMAL(18,2) DEFAULT 0,total_deductions DECIMAL(18,2) DEFAULT 0,net_payable DECIMAL(18,2) DEFAULT 0,working_days INT DEFAULT 26,paid_days INT DEFAULT 26,lop_days INT DEFAULT 0)`,
    `CREATE TABLE IF NOT EXISTS attendance(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,employee_id INT NOT NULL,month INT NOT NULL,year INT NOT NULL,day INT NOT NULL,status ENUM('P','A','H','L','HD') DEFAULT 'A',in_time VARCHAR(10),out_time VARCHAR(10),UNIQUE KEY uq_att(company_id,employee_id,month,year,day))`,
    `CREATE TABLE IF NOT EXISTS leave_types(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(100),days_allowed INT DEFAULT 0,is_paid TINYINT DEFAULT 1)`,
    `CREATE TABLE IF NOT EXISTS leaves(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,employee_id INT NOT NULL,leave_type VARCHAR(100),from_date DATE,to_date DATE,days INT DEFAULT 1,reason TEXT,status ENUM('pending','approved','rejected') DEFAULT 'pending',remarks TEXT,approved_by VARCHAR(100),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS bank_accounts(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,account_name VARCHAR(200) NOT NULL,account_number VARCHAR(50),ifsc_code VARCHAR(20),bank_name VARCHAR(200),branch_name VARCHAR(200),linked_ledger_id INT,opening_balance DECIMAL(18,2) DEFAULT 0,is_active TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS bank_transactions(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,bank_account_id INT NOT NULL,date DATETIME NOT NULL,description TEXT,amount DECIMAL(18,2) NOT NULL,transaction_type ENUM('credit','debit') NOT NULL,ref_number VARCHAR(100),voucher_id INT,is_reconciled TINYINT DEFAULT 0,reconciled_date DATETIME,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS cheque_entries(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,bank_account_id INT NOT NULL,cheque_number VARCHAR(30),cheque_date DATETIME,amount DECIMAL(18,2),payee_name VARCHAR(255),cheque_type ENUM('issued','received') DEFAULT 'issued',status ENUM('pending','cleared','bounced','cancelled') DEFAULT 'pending',voucher_id INT,remarks TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS fixed_assets(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200) NOT NULL,category VARCHAR(100),purchase_date DATETIME,cost DECIMAL(18,2),salvage_value DECIMAL(18,2) DEFAULT 0,useful_life_years INT DEFAULT 5,depreciation_method ENUM('SLM','WDV') DEFAULT 'SLM',accumulated_depreciation DECIMAL(18,2) DEFAULT 0,location VARCHAR(200),serial_number VARCHAR(100),is_disposed TINYINT DEFAULT 0,disposal_date DATETIME,disposal_value DECIMAL(18,2),created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS depreciation_entries(id INT AUTO_INCREMENT PRIMARY KEY,asset_id INT NOT NULL,date DATETIME NOT NULL,amount DECIMAL(18,2) NOT NULL,narration TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS maintenance_schedules(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,asset_id INT,title VARCHAR(255),type ENUM('preventive','corrective') DEFAULT 'preventive',frequency VARCHAR(50),last_done DATE,next_due DATE,assigned_to VARCHAR(100),priority ENUM('low','medium','high') DEFAULT 'medium',status VARCHAR(50) DEFAULT 'scheduled',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS maintenance_logs(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,asset_id INT,schedule_id INT,date DATE,description TEXT,cost DECIMAL(18,2) DEFAULT 0,performed_by VARCHAR(100),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS amc_tracker(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,asset_id INT,vendor VARCHAR(255),type VARCHAR(50) DEFAULT 'AMC',start_date DATE,expiry_date DATE,amount DECIMAL(18,2),notes TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS cost_centres(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200) NOT NULL,parent_centre_id INT,description TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS cost_allocations(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,cost_centre_id INT NOT NULL,voucher_id INT,ledger_id INT,amount DECIMAL(18,2) NOT NULL,date DATETIME,narration TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS currencies(id INT AUTO_INCREMENT PRIMARY KEY,code VARCHAR(10) UNIQUE NOT NULL,symbol VARCHAR(10),name VARCHAR(100),exchange_rate DECIMAL(18,6) DEFAULT 1,is_base TINYINT DEFAULT 0,updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS exchange_rate_history(id INT AUTO_INCREMENT PRIMARY KEY,currency_id INT NOT NULL,date DATETIME NOT NULL,rate DECIMAL(18,6) NOT NULL,narration TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS customers(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(255) NOT NULL,gstin VARCHAR(20),email VARCHAR(200),phone VARCHAR(20),address TEXT,credit_limit DECIMAL(18,2) DEFAULT 0,credit_days INT DEFAULT 30,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS vendors(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(255) NOT NULL,gstin VARCHAR(20),email VARCHAR(200),phone VARCHAR(20),address TEXT,credit_limit DECIMAL(18,2) DEFAULT 0,credit_days INT DEFAULT 30,created_at DATETIME DEFAULT CURRENT_TIMESTAMP,FOREIGN KEY(company_id)REFERENCES companies(id)ON DELETE CASCADE)`,
    `CREATE TABLE IF NOT EXISTS budgets(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200),period VARCHAR(50),start_date DATE,end_date DATE,ledger_id INT,amount DECIMAL(18,2) DEFAULT 0,type VARCHAR(50) DEFAULT 'ledger',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS crm_leads(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(255) NOT NULL,email VARCHAR(200),phone VARCHAR(20),company VARCHAR(255),source VARCHAR(100),status VARCHAR(50) DEFAULT 'new',assigned_to VARCHAR(100),follow_up_date DATE,notes TEXT,converted_customer_id INT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS pos_sessions(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,opened_by VARCHAR(100),opening_cash DECIMAL(18,2) DEFAULT 0,closing_cash DECIMAL(18,2),status ENUM('open','closed') DEFAULT 'open',opened_at DATETIME DEFAULT CURRENT_TIMESTAMP,closed_at DATETIME)`,
    `CREATE TABLE IF NOT EXISTS pos_sales(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,session_id INT,subtotal DECIMAL(18,2),discount DECIMAL(18,2) DEFAULT 0,total DECIMAL(18,2),payment_mode VARCHAR(50),amount_paid DECIMAL(18,2),change_amount DECIMAL(18,2) DEFAULT 0,billed_by VARCHAR(100),customer_name VARCHAR(200),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS pos_sale_items(id INT AUTO_INCREMENT PRIMARY KEY,pos_sale_id INT NOT NULL,stock_item_id INT,item_name VARCHAR(255),qty DECIMAL(18,4),rate DECIMAL(18,2),amount DECIMAL(18,2),gst_rate DECIMAL(5,2) DEFAULT 0,gst_amount DECIMAL(18,2) DEFAULT 0)`,
    `CREATE TABLE IF NOT EXISTS branches(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200) NOT NULL,address TEXT,gstin VARCHAR(20),state VARCHAR(100),phone VARCHAR(20),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS branch_transfers(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,from_branch_id INT,to_branch_id INT,transfer_type ENUM('stock','funds') DEFAULT 'stock',stock_item_id INT,qty DECIMAL(18,4),amount DECIMAL(18,2),date DATE,narration TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS service_masters(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200) NOT NULL,code VARCHAR(50),rate DECIMAL(18,2),unit VARCHAR(20),description TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS service_orders(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,customer_id INT,customer_name VARCHAR(255),service_id INT,description TEXT,priority ENUM('low','medium','high','urgent') DEFAULT 'medium',due_date DATE,assigned_to VARCHAR(100),estimated_cost DECIMAL(18,2),actual_cost DECIMAL(18,2),status VARCHAR(50) DEFAULT 'open',closed_at DATETIME,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS service_tickets(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,order_id INT,title VARCHAR(255),description TEXT,status VARCHAR(50) DEFAULT 'open',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS purchase_orders(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,vendor_id INT,vendor_name VARCHAR(255),date DATE,due_date DATE,total_amount DECIMAL(18,2),status VARCHAR(50) DEFAULT 'pending',notes TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS sales_orders(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,customer_id INT,customer_name VARCHAR(255),date DATE,due_date DATE,total_amount DECIMAL(18,2),status VARCHAR(50) DEFAULT 'pending',notes TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS order_items(id INT AUTO_INCREMENT PRIMARY KEY,order_id INT NOT NULL,order_type ENUM('purchase','sales') NOT NULL,stock_item_id INT,item_name VARCHAR(255),qty DECIMAL(18,4),rate DECIMAL(18,2),amount DECIMAL(18,2),gst_rate DECIMAL(5,2) DEFAULT 0,gst_amount DECIMAL(18,2) DEFAULT 0)`,
    `CREATE TABLE IF NOT EXISTS projects(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(255) NOT NULL,code VARCHAR(50),start_date DATE,end_date DATE,budget DECIMAL(18,2) DEFAULT 0,customer_id INT,status VARCHAR(50) DEFAULT 'active',description TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS project_costs(id INT AUTO_INCREMENT PRIMARY KEY,project_id INT NOT NULL,ledger_id INT,amount DECIMAL(18,2),date DATE,narration TEXT,cost_type VARCHAR(50) DEFAULT 'direct',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS project_revenues(id INT AUTO_INCREMENT PRIMARY KEY,project_id INT NOT NULL,amount DECIMAL(18,2),date DATE,narration TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS subscription_templates(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(255),amount DECIMAL(18,2),frequency VARCHAR(50),customer_id INT,description TEXT,next_billing_date DATE,is_active TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS subscription_register(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,template_id INT,billing_date DATE,amount DECIMAL(18,2),status ENUM('pending','paid','overdue') DEFAULT 'pending',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS eway_bills(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,voucher_id INT,ewb_number VARCHAR(50),generated_date DATE,valid_upto DATE,from_gstin VARCHAR(20),to_gstin VARCHAR(20),vehicle_number VARCHAR(20),transporter VARCHAR(255),total_value DECIMAL(18,2),hsn_code VARCHAR(20),status ENUM('active','cancelled') DEFAULT 'active',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS einvoices(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,voucher_id INT,irn VARCHAR(100),ack_no VARCHAR(50),ack_date DATE,qr_code LONGTEXT,party_gstin VARCHAR(20),total_value DECIMAL(18,2),status ENUM('active','cancelled') DEFAULT 'active',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS gst_filing_history(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,return_type VARCHAR(20),period VARCHAR(20),filed_date DATE,status VARCHAR(50) DEFAULT 'filed',tax_payable DECIMAL(18,2) DEFAULT 0,interest_penalty DECIMAL(18,2) DEFAULT 0,total_paid DECIMAL(18,2) DEFAULT 0,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS notifications(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT,title VARCHAR(255),message TEXT,type VARCHAR(50) DEFAULT 'info',target_user VARCHAR(100),is_read TINYINT DEFAULT 0,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS rules(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(255),type VARCHAR(100),condition_json TEXT,action_json TEXT,priority INT DEFAULT 1,is_enabled TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS rule_logs(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,rule_id INT,trigger_context TEXT,result TEXT,triggered_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS event_ledger(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,event_type VARCHAR(100),entity_type VARCHAR(100),entity_id INT,payload LONGTEXT,narration TEXT,created_by VARCHAR(100),event_time DATETIME DEFAULT CURRENT_TIMESTAMP,is_undone TINYINT DEFAULT 0,undone_by VARCHAR(100),undone_at DATETIME)`,
    `CREATE TABLE IF NOT EXISTS maker_checker(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,entity_type VARCHAR(100),entity_id INT,action VARCHAR(100),payload LONGTEXT,submitted_by VARCHAR(100),status ENUM('pending','approved','rejected') DEFAULT 'pending',approved_by VARCHAR(100),remarks TEXT,actioned_at DATETIME,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS voucher_comments(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,voucher_id INT,comment TEXT,created_by VARCHAR(100),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS tasks(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,title VARCHAR(255),description TEXT,voucher_id INT,assigned_to VARCHAR(100),due_date DATE,priority ENUM('low','medium','high') DEFAULT 'medium',status VARCHAR(50) DEFAULT 'open',created_by VARCHAR(100),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS ecommerce_orders(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,platform VARCHAR(100),order_id VARCHAR(100),customer_name VARCHAR(255),order_date DATE,total_amount DECIMAL(18,2),status VARCHAR(50) DEFAULT 'pending',converted_at DATETIME,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS ecommerce_order_items(id INT AUTO_INCREMENT PRIMARY KEY,ecommerce_order_id INT NOT NULL,sku VARCHAR(100),item_name VARCHAR(255),qty DECIMAL(18,4),rate DECIMAL(18,2),amount DECIMAL(18,2))`,
    `CREATE TABLE IF NOT EXISTS ecommerce_config(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,platform VARCHAR(100),api_key LONGTEXT,shop_url VARCHAR(255),is_active TINYINT DEFAULT 1,UNIQUE KEY uq_ec(company_id,platform))`,
    `CREATE TABLE IF NOT EXISTS whatsapp_config(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT UNIQUE NOT NULL,api_key LONGTEXT,phone_number VARCHAR(20),business_id VARCHAR(100),is_active TINYINT DEFAULT 0)`,
    `CREATE TABLE IF NOT EXISTS whatsapp_queue(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,recipient VARCHAR(20),message TEXT,type VARCHAR(50) DEFAULT 'general',scheduled_at DATETIME,status ENUM('queued','sent','failed') DEFAULT 'queued',sent_at DATETIME,error TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS custom_fields(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,entity VARCHAR(100),field_name VARCHAR(100),field_type VARCHAR(50) DEFAULT 'text',is_required TINYINT DEFAULT 0,default_value TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS customization_config(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT UNIQUE NOT NULL,config_json LONGTEXT)`,
    `CREATE TABLE IF NOT EXISTS workflows(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(255),trigger_event VARCHAR(100),steps_json LONGTEXT,is_enabled TINYINT DEFAULT 1,created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS tally_imports(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,file_name VARCHAR(255),total_records INT DEFAULT 0,imported_records INT DEFAULT 0,errors_json LONGTEXT,source_type VARCHAR(20) DEFAULT 'XML',status VARCHAR(50) DEFAULT 'completed',imported_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS audit_log(id INT AUTO_INCREMENT PRIMARY KEY,username VARCHAR(100),action VARCHAR(100),entity VARCHAR(100),entity_id INT,details LONGTEXT,ip_address VARCHAR(50),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS data_backups(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT,filename VARCHAR(255),size_kb INT,type VARCHAR(50) DEFAULT 'manual',created_by VARCHAR(100),created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS app_settings(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT,setting_key VARCHAR(100) NOT NULL,setting_value LONGTEXT,updated_by VARCHAR(100),updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,UNIQUE KEY uq_setting(company_id,setting_key))`,
    `CREATE TABLE IF NOT EXISTS premium_features(id INT AUTO_INCREMENT PRIMARY KEY,feature_key VARCHAR(100) UNIQUE NOT NULL,feature_name VARCHAR(200),is_enabled TINYINT DEFAULT 1,updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS contact_queries(id INT AUTO_INCREMENT PRIMARY KEY,name VARCHAR(200),email VARCHAR(200),phone VARCHAR(20),subject VARCHAR(255),message TEXT,reply TEXT,replied_by VARCHAR(100),replied_at DATETIME,status ENUM('open','replied','closed') DEFAULT 'open',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`,
    `CREATE TABLE IF NOT EXISTS invoice_templates(id INT AUTO_INCREMENT PRIMARY KEY,company_id INT NOT NULL,name VARCHAR(200),template_html LONGTEXT,is_default TINYINT DEFAULT 0,accent_color VARCHAR(20),show_logo TINYINT DEFAULT 1,show_signature TINYINT DEFAULT 1,show_gst TINYINT DEFAULT 1,show_bank TINYINT DEFAULT 1,paper_size VARCHAR(20) DEFAULT 'A4',created_at DATETIME DEFAULT CURRENT_TIMESTAMP)`
  ];
  for(const t of tables)await c.execute(t);
  console.log('[MIGRATE] All 55+ tables created successfully.');
  await c.end();
}
migrate().catch(e=>{console.error('[MIGRATE] Failed:',e.message);process.exit(1);});

const router=require('express').Router(),db=require('../database/db'),{authenticate}=require('../middleware/auth');
router.get('/',authenticate,async(req,res)=>{const{companyId}=req.query;const[r]=await db.query('SELECT * FROM vendors WHERE company_id=? ORDER BY name',[companyId]);res.json(r);});
router.post('/',authenticate,async(req,res)=>{const{companyId,name,gstin,email,phone,address,creditLimit,creditDays}=req.body;const[r]=await db.query('INSERT INTO vendors(company_id,name,gstin,email,phone,address,credit_limit,credit_days)VALUES(?,?,?,?,?,?,?,?)',[companyId,name,gstin,email,phone,address,creditLimit||0,creditDays||30]);const[rows]=await db.query('SELECT * FROM vendors WHERE id=?',[r.insertId]);res.json(rows[0]);});
router.put('/:id',authenticate,async(req,res)=>{const{name,gstin,email,phone,address,creditLimit,creditDays}=req.body;await db.query('UPDATE vendors SET name=?,gstin=?,email=?,phone=?,address=?,credit_limit=?,credit_days=? WHERE id=?',[name,gstin,email,phone,address,creditLimit,creditDays,req.params.id]);const[r]=await db.query('SELECT * FROM vendors WHERE id=?',[req.params.id]);res.json(r[0]);});
router.delete('/:id',authenticate,async(req,res)=>{await db.query('DELETE FROM vendors WHERE id=?',[req.params.id]);res.json({message:'Deleted'});});
router.get('/:id/ledger',authenticate,async(req,res)=>{const{companyId}=req.query;const[vend]=await db.query('SELECT * FROM vendors WHERE id=?',[req.params.id]);if(!vend.length)return res.status(404).json({error:'Not found'});const[led]=await db.query('SELECT * FROM ledgers WHERE company_id=? AND name=?',[companyId,vend[0].name]);const ledgerId=led.length?led[0].id:null;let entries=[];if(ledgerId){const[rows]=await db.query('SELECT v.date,v.voucher_type,v.voucher_number,v.narration,ve.amount,ve.entry_type FROM vouchers v JOIN voucher_entries ve ON v.id=ve.voucher_id WHERE v.company_id=? AND ve.ledger_id=? ORDER BY v.date',[companyId,ledgerId]);entries=rows;}let balance=0;entries.forEach(e=>{balance+=e.entry_type==='CR'?+e.amount:-+e.amount;});res.json({vendor:vend[0],entries,balance});});
module.exports=router;

import { api } from '../apiClient';
const actor = {
  verifyUser: async (u: string, p: string) => { try { const r = await api.login(u, p); if ((r as any).token) { localStorage.setItem('hk_jwt_token', (r as any).token); return (r as any).user; } return null; } catch { return null; } },
  getAllUsers: () => api.getUsers(),
  createUser: (d: any) => api.createUser(d),
  updateUser: (id: any, d: any) => api.updateUser(Number(id), d),
  deleteUser: (id: any) => api.deleteUser(Number(id)),
  resetUserPassword: (id: any, p: string) => api.resetUserPassword(Number(id), p),
  saveUserProfile: (_u: string, d: any) => api.updateProfile(d),
  getUserProfile: (_u: string) => api.getProfile(),
  getAllCompanies: () => api.getCompanies(),
  createCompany: (d: any) => api.createCompany(d),
  updateCompany: (id: any, d: any) => api.updateCompany(Number(id), d),
  deleteCompany: (id: any) => api.deleteCompany(Number(id)),
  getAllLedgerGroups: () => api.getLedgerGroups(),
  createLedgerGroup: (d: any) => api.createLedgerGroup(d),
  updateLedgerGroup: (id: any, d: any) => api.updateLedgerGroup(Number(id), d),
  getAllLedgers: (cid?: any) => api.getLedgers(cid ? Number(cid) : undefined),
  createLedger: (d: any) => api.createLedger(d),
  updateLedger: (id: any, d: any) => api.updateLedger(Number(id), d),
  deleteLedger: (id: any) => api.deleteLedger(Number(id)),
  getTrialBalance: (cid: any) => api.getTrialBalance(Number(cid)),
  getAllVouchers: (cid: any, type?: string) => api.getVouchers(Number(cid), type),
  getVoucher: (id: any) => api.getVoucher(Number(id)),
  createVoucher: (d: any) => api.createVoucher(d),
  updateVoucher: (id: any, d: any) => api.updateVoucher(Number(id), d),
  deleteVoucher: (id: any) => api.deleteVoucher(Number(id)),
  getDayBook: (cid: any, date?: string) => api.getDayBook(Number(cid), date),
  getGSTSettings: (cid: any) => api.getGSTSettings(Number(cid)),
  saveGSTSettings: (cid: any, d: any) => api.saveGSTSettings(Number(cid), d),
  getAllGSTVouchers: (cid: any) => api.getGSTVouchers(Number(cid)),
  createGSTVoucher: (d: any) => api.createGSTVoucher(d),
  getGSTR1Report: (cid: any, m: string, y: string) => api.getGSTR1(Number(cid), m, y),
  getGSTR3BReport: (cid: any, m: string, y: string) => api.getGSTR3B(Number(cid), m, y),
  getAllHSNCodes: () => api.getHSNCodes(),
  createHSNCode: (d: any) => api.createHSNCode(d),
  updateHSNCode: (id: any, d: any) => api.updateHSNCode(Number(id), d),
  getAllStockGroups: () => api.getStockGroups(),
  createStockGroup: (d: any) => api.createStockGroup(d),
  getAllStockItems: (cid?: any) => api.getStockItems(cid ? Number(cid) : undefined),
  createStockItem: (d: any) => api.createStockItem(d),
  updateStockItem: (id: any, d: any) => api.updateStockItem(Number(id), d),
  getStockSummary: (cid: any) => api.getStockSummary(Number(cid)),
  getStockLedger: (cid: any, iid: any) => api.getStockLedger(Number(cid), Number(iid)),
  getAllPayrollVouchers: (cid: any) => api.getPayrollVouchers(Number(cid)),
  getPayrollVoucher: (id: any) => api.getPayrollVoucher(Number(id)),
  createPayrollVoucher: (d: any) => api.createPayrollVoucher(d),
  getAllSalaryStructures: (cid: any) => api.getSalaryStructures(Number(cid)),
  getSalaryStructure: (cid: any, eid: any) => api.getSalaryStructure(Number(cid), Number(eid)),
  saveSalaryStructure: (d: any) => api.saveSalaryStructure(d),
  getAllEmployees: (cid: any) => api.getEmployees(Number(cid)),
  createEmployee: (d: any) => api.createEmployee(d),
  updateEmployee: (id: any, d: any) => api.updateEmployee(Number(id), d),
  getAllBankAccounts: (cid: any) => api.getBankAccounts(Number(cid)),
  createBankAccount: (d: any) => api.createBankAccount(d),
  updateBankAccount: (id: any, d: any) => api.updateBankAccount(Number(id), d),
  getBankStatement: (aid: any) => api.getBankTransactions(Number(aid)),
  getChequesByBankAccount: (aid: any) => api.getCheques(Number(aid)),
  createChequeEntry: (d: any) => api.createCheque(d),
  updateChequeStatus: (id: any, s: string, _r: string) => api.updateChequeStatus(Number(id), s),
  getAllFixedAssets: (cid: any) => api.getFixedAssets(Number(cid)),
  createFixedAsset: (d: any) => api.createFixedAsset(d),
  updateFixedAsset: (id: any, d: any) => api.updateFixedAsset(Number(id), d),
  deleteFixedAsset: (id: any) => api.deleteFixedAsset(Number(id)),
  getAllCostCentres: (cid: any) => api.getCostCentres(Number(cid)),
  createCostCentre: (d: any) => api.createCostCentre(d),
  getAllCurrencies: () => api.getCurrencies(),
  createCurrency: (d: any) => api.createCurrency(d),
  getBalanceSheet: (cid: any) => api.getBalanceSheet(Number(cid)),
  getPLAccount: (cid: any) => api.getPL(Number(cid)),
  getCashFlowStatement: (cid: any) => api.getCashFlow(Number(cid)),
  getAllCustomers: (cid: any) => api.getCustomers(Number(cid)),
  createCustomer: (d: any) => api.createCustomer(d),
  updateCustomer: (id: any, d: any) => api.updateCustomer(Number(id), d),
  getAllVendors: (cid: any) => api.getVendors(Number(cid)),
  createVendor: (d: any) => api.createVendor(d),
  updateVendor: (id: any, d: any) => api.updateVendor(Number(id), d),
  getAllPurchaseOrders: (cid: any) => api.getPurchaseOrders(Number(cid)),
  createPurchaseOrder: (d: any) => api.createPurchaseOrder(d),
  getAllSalesOrders: (cid: any) => api.getSalesOrders(Number(cid)),
  createSalesOrder: (d: any) => api.createSalesOrder(d),
  getAllNotifications: () => api.getNotifications(),
  markNotificationRead: (id: any) => api.markNotificationRead(Number(id)),
  getSmartAlerts: (cid: any) => api.getSmartAlerts(Number(cid)),
};
export function useActor() { return { actor: actor as any, isFetching: false }; }
